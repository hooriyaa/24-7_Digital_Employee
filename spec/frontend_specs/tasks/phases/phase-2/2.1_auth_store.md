```slc
@block TASK 2.1_auth_store
priority: critical
intent: "Implement authentication store with Zustand"
scope: phase-2
depends_on: 1.11
estimate: 10
status: todo

content:
  - Update stores/authStore.ts with complete implementation
  - Add login(credentials) action:
    - Call POST /auth/login
    - Store tokens
    - Decode and store user info
  - Add logout() action:
    - Clear tokens and user
    - Redirect to login
  - Add refreshToken() action:
    - Call POST /auth/refresh
    - Update stored tokens
  - Add persist middleware for localStorage
  - Add auto-refresh token timer
  - Test login/logout flow

acceptance_criteria:
  - Login stores tokens
  - Logout clears state
  - Token refresh works
  - State persists across reloads
  - Auto-refresh scheduled
@end
```
