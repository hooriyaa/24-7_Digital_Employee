```slc
@block TASK 2.3_auth_guards
priority: critical
intent: "Implement authentication guards and route protection"
scope: phase-2
depends_on: 2.2
estimate: 10
status: todo

content:
  - Create components/auth/AuthGuard.tsx
  - Implement route protection logic:
    - Check isAuthenticated from store
    - Redirect to /login if not authenticated
    - Show loading while checking
  - Create app/(dashboard)/layout.tsx with AuthGuard
  - Implement redirect logic:
    - Authenticated users redirected from /login to /dashboard
  - Add middleware for server-side protection (optional)
  - Test protected route access

acceptance_criteria:
  - Unauthenticated users redirected
  - Protected routes accessible when logged in
  - Login redirects authenticated users
  - Loading state during check
  - No flash of unauthenticated content
@end
```
