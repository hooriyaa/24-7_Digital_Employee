```slc
@block TASK 1.6_zustand_store
priority: high
intent: "Setup Zustand for client state management"
scope: phase-1
depends_on: 1.5
estimate: 10
status: todo

content:
  - Install zustand
  - Create stores/ directory
  - Create stores/authStore.ts with:
    - user state
    - token state
    - isAuthenticated computed
    - login action
    - logout action
  - Create stores/ticketStore.ts with:
    - tickets array
    - filters state
    - setTickets action
    - setFilters action
  - Create stores/index.ts for store exports
  - Test store usage in components

acceptance_criteria:
  - Zustand installed
  - Auth store functional
  - Ticket store functional
  - Stores accessible in components
  - State persists correctly
@end
```
