```slc
@block TASK 4.1_k8s_deployment
priority: critical
intent: "Create Kubernetes deployment manifests"
scope: phase-4
depends_on: 3.14
estimate: 15
status: todo

content:
  - Call Context7 MCP for Kubernetes best practices
  - Create k8s/ directory
  - Create k8s/deployment.yaml for backend API
  - Configure replicas (min 2 for HA)
  - Configure resource limits (CPU, memory)
  - Configure liveness and readiness probes
  - Configure rolling update strategy
  - Test manifest validation with kubectl

acceptance_criteria:
  - k8s directory created
  - deployment.yaml valid
  - Replicas configured
  - Resource limits set
  - Probes configured
  - kubectl apply succeeds
@end
```
