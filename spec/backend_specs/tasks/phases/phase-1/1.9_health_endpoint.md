```slc
@block TASK 1.9_health_endpoint
priority: high
intent: "Create health check endpoint"
scope: phase-1
depends_on: 1.8
estimate: 10
status: todo

content:
  - Create api/routes.py
  - Implement GET /health endpoint
  - Return status, timestamp, version
  - Add database connectivity check
  - Add Kafka connectivity check
  - Create schemas/health.py with HealthResponse model
  - Test endpoint returns 200 with all checks

acceptance_criteria:
  - /health endpoint accessible
  - Response includes status, timestamp, version
  - Database check included
  - Kafka check included
  - Returns 503 if any check fails
@end
```
