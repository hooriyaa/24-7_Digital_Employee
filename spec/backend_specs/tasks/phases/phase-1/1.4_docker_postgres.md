```slc
@block TASK 1.4_docker_postgres
priority: critical
intent: "Create Docker configuration for PostgreSQL with pgvector"
scope: phase-1
depends_on: 1.3
estimate: 15
status: todo

content:
  - Create docker-compose.yml
  - Add PostgreSQL service with:
    - Image: pgvector/pgvector:pg15
    - Environment: POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB
    - Ports: 5432:5432
    - Volume: postgres_data:/var/lib/postgresql/data
  - Enable pgvector extension in init script
  - Create Dockerfile for backend service
  - Test PostgreSQL connection with pgvector enabled

acceptance_criteria:
  - docker-compose.yml is valid YAML
  - PostgreSQL container starts successfully
  - pgvector extension is enabled
  - Connection test succeeds
@end
```
