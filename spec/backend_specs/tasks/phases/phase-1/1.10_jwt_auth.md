```slc
@block TASK 1.10_jwt_auth
priority: high
intent: "Implement JWT authentication system"
scope: phase-1
depends_on: 1.9
estimate: 15
status: todo

content:
  - Create core/security.py with JWT functions:
    - create_access_token()
    - verify_token()
    - hash_password()
    - verify_password()
  - Create schemas/token.py with Token and TokenData models
  - Create schemas/user.py with UserCreate and UserResponse
  - Create api/deps.py with get_current_user dependency
  - Implement POST /auth/login endpoint
  - Implement POST /auth/refresh endpoint
  - Add rate limiting to auth endpoints
  - Test token creation and validation

acceptance_criteria:
  - JWT tokens created and verified
  - Password hashing working
  - get_current_user dependency functional
  - Login returns access token
  - Refresh returns new token
  - Invalid tokens rejected with 401
@end
```
