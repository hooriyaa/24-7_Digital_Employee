```slc
@block TASK 3.2_kafka_consumer
priority: critical
intent: "Implement Kafka consumer for event processing"
scope: phase-3
depends_on: 3.1
estimate: 15
status: todo

content:
  - Create services/kafka/consumer.py
  - Implement KafkaConsumerService class
  - Implement subscribe(topics: List[str]) method
  - Implement consume() async generator
  - Create event handlers for each topic
  - Handle tickets.create events to trigger AI agent
  - Handle messages.send events to deliver responses
  - Implement consumer group configuration
  - Test consuming from all topics

acceptance_criteria:
  - Consumer service functional
  - Subscription working
  - Events consumed asynchronously
  - tickets.create triggers agent
  - messages.send delivers responses
  - Consumer group configured
  - Error handling and reconnection
@end
```
