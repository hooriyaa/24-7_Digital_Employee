```slc
@block TASK 3.8_ticket_crud
priority: critical
intent: "Implement ticket CRUD endpoints"
scope: phase-3
depends_on: 3.7
estimate: 15
status: todo

content:
  - Create schemas/ticket.py with all ticket schemas
  - Implement POST /tickets endpoint
  - Implement GET /tickets endpoint with pagination
  - Implement GET /tickets/{ticket_id} endpoint
  - Implement PUT /tickets/{ticket_id} endpoint
  - Implement POST /tickets/{ticket_id}/close endpoint
  - Add request validation with Pydantic
  - Add response filtering and formatting
  - Test all ticket endpoints

acceptance_criteria:
  - All ticket schemas defined
  - Create ticket works
  - List tickets with pagination works
  - Get single ticket works
  - Update ticket works
  - Close ticket works
  - Validation errors returned correctly
@end
```
