```slc
@block TASK 2.5_escalation_rules
priority: high
intent: "Configure escalation rules and thresholds"
scope: phase-2
depends_on: 2.4
estimate: 10
status: todo

content:
  - Create services/escalation/rules.py
  - Implement should_escalate(ticket: Ticket) -> bool method
  - Rule 1: confidence_score < 0.7
  - Rule 2: sentiment_score < -0.5
  - Rule 3: message contains "speak to human" or similar
  - Rule 4: message contains pricing/billing keywords
  - Create EscalationReason enum with all reason types
  - Integrate escalation check into AI response flow
  - Test each escalation rule independently

acceptance_criteria:
  - All 4 escalation rules implemented
  - should_escalate() returns correct boolean
  - EscalationReason enum defined
  - Rules integrated into response flow
  - Each rule testable independently
  - Escalation triggered when any rule matches
@end
```
