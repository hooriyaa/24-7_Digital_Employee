```slc
@block TASK 2.8_vector_embeddings
priority: critical
intent: "Implement pgvector embeddings for messages"
scope: phase-2
depends_on: 2.7
estimate: 15
status: todo

content:
  - Call Context7 MCP for pgvector documentation
  - Verify pgvector extension enabled in database
  - Create services/embeddings/embedder.py
  - Implement create_embedding(text: str) -> List[float] method
  - Use OpenAI text-embedding-ada-002 or similar
  - Store embeddings in Message.content_embedding (VECTOR(1536))
  - Integrate embedding creation into message save flow
  - Test embedding generation and storage

acceptance_criteria:
  - pgvector extension enabled
  - create_embedding() returns 1536-dim vector
  - Embeddings stored in database
  - Message model accepts vector
  - Embedding generation tested
  - Error handling for embedding failures
@end
```
